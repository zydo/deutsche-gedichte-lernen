# 诗歌编辑工作规范（Editorial Workflow）

本文件规定**如何选择、研究、录入、校对、验证与发布一首诗**。它不是本批次的工作日志，而是后续每次扩容都要重复执行的操作规范。

- 来源独立性与来源分级的唯一细则：[`SOURCES.md`](SOURCES.md)
- 数据字段与本地运行入口：[`README.md`](README.md)
- 本文件：从“候选诗”到“可发布页面”的端到端流程

> 核心原则：先锁定有 provenance 的正文，再写译文与讲解。不得从记忆、模型输出、用户提示中的片段或其他诗页反向拼正文。

---

## 1. 建立候选记录，不先抄正文

在检索前先记录：

1. 诗题、作者、预定 slug 与未占用的连续编号；
2. 选择理由：时间轴、流派、形式、作者结构或语言教学价值；
3. 作者卒年与德国公版起始年（`卒年 + 71`）；
4. 预计的高风险点：异文、旧正字法、方言/中古德语、自造词、无作者定本、选段而非完整作品；
5. 需要验证的断言，不把“通行说法”预先写成事实。

尚未公版的作品只能放在 `data/poems/pending/`：`published: false`、`release_date` 明确、`german_text` 为空。不得以“草稿”“测试 fixture”或注释引文为名提前写入正文。

---

## 2. 建立来源矩阵

从编号 34 起，每首新诗必须满足：

- 至少 **3 个独立运营方**；
- 至少 **1 个一级来源**；
- 每条来源显式写 `tier`；
- 一级来源写 `citation`，包含纸本版次与页码；
- 三级聚合站只能辅助，不得补足必需的独立来源数。

推荐先在 `.localonly/` 建研究矩阵（该目录不进版本库）：

| 字段                | 内容                               |
| ------------------- | ---------------------------------- |
| URL / 运营方 / tier | 按 `SOURCES.md` 分类               |
| 纸本底本            | 编者、书名、卷、出版地、年份、页码 |
| 行数与分节          | 每源分别记录                       |
| 实词异文            | 逐行并列                           |
| 标点/省音/正字法    | 逐行并列                           |
| 采用理由            | 为什么选此源为正文底本             |
| 未决问题            | 不能确认的内容，不得“合理补全”     |

### 抓取顺序

1. 先找一级来源的作者全集、校勘版或可靠数字化版本；
2. 再找两个不同运营方的独立转录；
3. 实际打开并读取页面，不把搜索结果摘要当正文；
4. 用 `npm run snapshot` 保存 URL、原始字节、时间与 SHA-256；
5. 抓不到就换来源。任何缺行都不得由模型补写。

一级来源也可能有转录错误。若内证冲突，记录三源读法、上下文与判断依据；不要因为“一级”标签而静默覆盖其他证据。

---

## 3. 锁定正文版本

只在来源矩阵完成后录入 `german_text`。

### 3.1 普通作者作品

- 明确采用哪一版；
- 逐行比较三个来源；
- 所有实词、标点、分节、大小写、缩写与省音差异进入 `verification_notes`；
- “未发现异文”也要写；
- 页面只保留一个当前状态。历史变化放在【沿革】，最后一段写现状，checklist 与现状同步。

### 3.2 19 世纪及以前

正文采用现代化正字法通行文本。原刊形态完整记录在校对记录中。只有旧拼法确实承载押韵、音值或格律信息时才保留，并逐项解释。页面若声明现代化，就不能残留未经说明的 `muß`、`daß`、`laß` 等旧拼法。

### 3.3 民歌与匿名作品

民歌没有作者定本。不得把某一通行唱本称为“正确原文”：

- 并列各来源的节数与异文；
- 说明本站采用哪一唱本及教学理由；
- 把异文视为传播史证据，而不是需要消灭的错误。

### 3.4 中古高地德语

- Mhd. 原文必须来自注明编号/抄本体系的来源；
- `text_nhd` 只有在现代转写也有外部来源时才启用；
- 不自行把 Mhd. 改写成 Nhd.；
- Mhd.、Nhd.、中文三栏必须逐节逐行等长。

### 3.5 选段

页眉、collection、来源与校对记录都必须写明“选段”，并列出每段可核验的卷次编号。不得用完整作品的标题让读者误以为页面收录了全篇。

### 3.6 节选（长诗截取）

当一首诗过长（如 Bürger《Lenore》256 行），须按以下四条处理，处理口径以本站《智天使漫游者》（选 3 则）与《蕾诺勒》（节选 7 节 / 全诗 32 节）为准：

1. **节选范围必须有 provenance**——不得由模型自行截取。优先采用德语教材／选本已有的通行节选段落；若无，则**一律按完整诗节边界切分**，不在诗节内部截断，并在校对记录中说明切分依据与所依据的版本页码。
2. **页眉、Quellen、Checklist 三处必须明确标注「节选 N 节 / 全诗 N 节」**，不得让读者误以为这是全诗；逐行注释另标出各节在全诗中的原节次。
3. **被省略的部分须给出一段中文情节梗概**，保证节选可读。
4. 与本站既有节选页（如《智天使漫游者》）保持口径一致。

### 3.7 同名作品（不同作者的同题诗）

德语诗中同名作品极多（《Weltende》《Herbst》《Abendlied》《An die Nacht》等都有大量同名之作）。本站首次出现该情况于第六批：van Hoddis《Weltende》（编号33）与 Lasker-Schüler《Weltende》（编号57）是两位作者的两首不同作品。处理规范：

1. **slug 加作者后缀**：后一首的 slug 必须为 `<标题>-<作者标识>`（如 `weltende-lasker-schueler`），不得占用或改动既有的同名 slug。
2. **双向消歧声明**：两页的页眉、第一条逐行注释、文化背景都必须显式写出「本站另收有 X / Y 的同名诗，是不同作者的不同作品，非异文」，并经 `cross_references` 双向声明。
3. **`title_zh` 作区分**：两首的中译题用不同措辞（如《世界末日》vs《世界之终》），全站互指一律使用同一套称呼，不得一处一个叫法。
4. **目录页可见性须浏览器实测**：上线前确认目录页与站内搜索中两首都能被列出，且作者名可见——不得只在数据层消歧而在展示层把两首混成一首。

---

## 4. 生成逐行审计表

从锁定后的 JSON 打印连续行号，至少生成以下四列：

| 行号 | 德文 | 中文 | Nhd.（如有） |
| ---- | ---- | ---- | ------------ |

逐行检查：

1. 节数相同；
2. 每节行数相同；
3. 没有整行前移/后移；
4. 注释中的引文确实命中对应原诗行；
5. 中文没有把相邻两行的谓语、否定或修饰关系交叉放错。

构建 lint 会检查行数结构，但不会判断译义是否错位；因此仍需人工逐行读一遍生成后的 HTML。

---

## 5. 自验形式，不抄标签

### 韵式

1. 写出每行韵脚；
2. 第一次出现的新韵分配新字母；
3. 同韵沿用字母；
4. 跨节换韵时重新核实，不能让同一字母指代不同声音；
5. 最后才把韵式写入页面。

不得写“（近似）”。正文或页眉只要提到 Kreuzreim、Sonett、Alexandriner 等，就必须在语法/形式说明中给出正文证据。

无尾韵的作品明确写“本诗无韵式”，并解释无韵如何构成形式。不要把“无韵”误写成“没有形式”。

### 格律与行长

- 音步必须用至少一至两行切分验证；
- Alexandriner 核实六音步、Zäsur 与阴/阳韵；
- Langzeile 给出至少两行音节数；
- 分节统一写“节数 × 每节行数”，如 `5×4`；不写反。

---

## 6. 写学习内容

顺序固定为：逐行注释 → 词汇 → 变位 → 语法 → 背景 → 译注 → image brief。

### 6.1 数字断言

只要写“3 例 / 三次 / 10 个韵词 / 共四处”等计数，就在同句或紧邻括号给出行号清单，且**行号项数必须等于数字**。同一行有两个实例时，行号可以重复，例如“第1、1行”。

不要为躲 lint 改用含糊表述；如果计数没有教学价值，可以删掉数字，改为逐项点名。

### 6.2 词汇与动词

- 词汇和变位表中的 lemma 必须在正文实际出现（自造词页除外，见下）；
- 可分/不可分、强/弱/混合变化、格支配、虚拟式式别在词条、变位表、逐行注释三处一致；
- `bringen / denken / kennen / nennen / brennen / senden / wenden` 等 Rückumlaut 类写“混合变化”，不归成强或弱；
- 诗人自造的非规范词形不进 Wortschatz，只收其规范基础词，并标“诗人自造，不可套用”。

### 6.3 跨页接线

跨页对照不是凭印象写 prose。先添加：

```json
"cross_references": [
  {
    "target_slug": "target-poem",
    "needle": "目标页必须实际含有的术语或词形",
    "context": "本页断言句中用于定位的诗题"
  }
]
```

然后才写“本站《……》中亦出现 / 可对照”等句子。lint 会在目标 JSON 中检索 `needle`，失败时输出“断言页 / 被点名页 / 未命中词”。

维护全站清单时，各页排除自身并保持内容一致。表外接点必须先全文检索。不得把“目标诗中恰好有该词”冒充“目标页面已标注该现象”。

### 6.4 韵式（构建期强制）

韵式必须**自己逐行数韵脚后**写入，不得沿用现成说法，不得写“（近似）”。字母体系：每出现一个新韵给新字母，重复的韵沿用旧字母。

写成 `韵式 abab cdcd` 这种形式（`韵式` 后直接跟字母串）即触发校验：lint 从 `german_text` 取每行末词、按韵尾聚类、转成规范字母串与声明比对，不一致就报错并打印逐行末词清单。声明可以覆盖全诗，也可以只覆盖**连续若干个完整诗节**（十四行诗只讲两个四行节是合法的），或写成 `第3节韵式 …` 显式限定。

三种合法写法：全诗连续编字母（跨节复用同韵）、逐节重新编字母、各节同型时只写一节。

四类逃生门，都必须显式声明，不得留空蒙混：

| 情况               | 写法                                                                        |
| ------------------ | --------------------------------------------------------------------------- |
| 叠句等不入韵的行   | `"rhyme_exempt_lines": [8, 17]`（全诗 1 起的行号），韵式中该位可写 `–`      |
| 押韵不规则         | `"rhyme_scheme": "irregular"`，且正文必须写明“押韵不规则”并给出实际韵脚清单 |
| 本诗无韵           | 正文明写“本诗没有韵式”（如 `prometheus`、`patrouille`）                     |
| 自动聚类判不出的韵 | `"rhyme_groups": [[30, 31]]`                                                |

`rhyme_groups` 用于两种情形：**不纯韵**（`stille`/`Hülle`、`für`/`dir`，诗人当作一个韵）和**非重读词尾**（`schnurrend`/`murrend` 的 -end 不重读，聚类会误并到 `behend`/`rennt` 的重读 -end 上——重音位置无法由拼写推出）。

**反向规则同样是 ERROR**：`tags` 里出现 `十四行`、`交叉韵`、`抱韵`、`亚历山大体`、`韵脚`、`长行诗`、`叠句`、`对句` 等形式标签，正文却没有任何韵式或格律表述，即“页眉声明了形式，正文未给出”。页眉承诺什么，正文就得兑现什么。

声明中若有字母只出现一次（该行不与任何行相押），lint 会打印 ⚠️ 提示要求确认——这类是最常见的错法（把互押的行误标成互不押韵）。孤韵有时是真的：`die-nachtigall` 的 `Kind` 确实不入韵，而这恰好是分辨原诗与贝尔格谱曲改词版（`Blut`，押住 `Sommerhut`/`Glut`）的形式证据。

### 6.5 高危断言

谱曲次数、作品编号、名人引语、影视引用、稿本数量、最高级评价、具体生平地点等：

- 有可靠来源：在来源或句内说明；
- 无法回核：写“（待进一步核实）”；
- 对理解无帮助：删除，不用“据说”保留。

---

## 7. 填写来源、校对记录与 checklist

每条 `german_sources` 至少包括：

```json
{
  "name": "来源名称",
  "url": "https://…",
  "tier": 1,
  "citation": "纸本校勘版，卷次，年份，S. 123",
  "note": "本次逐行比对中承担什么作用"
}
```

`verification_notes` 推荐结构：

1. 【底本与处理原则】
2. 【三源逐行比对】
3. 【异文与本站取舍】
4. 【沿革】（如有）
5. 【待进一步核实】
6. 最后一段：唯一的当前状态

Checklist 至少覆盖：作者/题名/出处、三源独立性、一级版次页码、行数/分节、逐行对齐、韵式/格律、正字法、词汇 lemma、变位一致、公版年份、image brief、视觉检查与最终人工复核。

---

## 8. 构建与发布门槛

按顺序运行：

```bash
npm test
npm run check
npm run snapshot
npm run build
npm run format:check
```

### `npm run check` 必须覆盖

- 来源独立性与 tier；
- 数字—行号一致；
- 跨页断言命中；
- 德中/Nhd. 逐节逐行等长；
- 公版年份复算；
- pending 正文禁入；
- 编号与 slug 唯一；
- 幽灵 token；
- 韵式自校验（正向与反向，见 6.4）；
- 已知的前置第二格 Hölderlin 伪清单回归。

### 浏览器回归

至少检查：

1. 首页作者顺序、诗数与 CEFR；详情页的注释密度；
2. about 的作者/匿名/时间跨度统计；
3. 新诗的长标题、长行、极短行与来源折叠；
4. Mhd. 三栏（如启用）；
5. 随机 5 首旧诗仍是原来的两栏 DOM；
6. 上一首/下一首仍按 id，01–33 未重排；
7. 每首新诗的生成 HTML 再做一次德中逐行核对。

任何 lint、抓取或视觉检查被跳过，都必须在交付说明中明确写“未执行”，不能用“应当通过”代替结果。

---

## 9. 发布后的维护

- 来源网页变化：保留旧快照，重新抓取后记录差异；
- 新增主题接点：先写结构化引用，再改 prose 与反向清单；
- 修正文：同步检查译文、行注、词条、韵式、变位与所有行号；
- 新公版作品：到 `release_date` 后先重新执行完整来源流程，填入正文，再把 `published` 改为 `true`；
- 不自动提交或推送。提交、推送与发布必须由维护者明确发出指令。
